import { Popup } from "../Popup";

export class ButtonMenuPopup extends Popup {
	protected override className = "ButtonMenu";

	/* 	protected className = "ButtonMenuPopup"; */
	/* 	GetPlayerFrame(player: Player): Frame {
		return this.GetPlayerFrame(player);
	}
	Show(player: Player): void {
		this.Show(player);
	}
	Hide(player: Player): void {
		this.Hide(player);
	} */
}
